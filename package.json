{
  "name": "kinetix",
  "version": "1.0.0",
  "description": "Kinetix - Unified Platform for fitness coaching (mobile app + web dashboard)",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/mobile",
    "packages/shared",
    "packages/ai-models",
    "packages/core",
    "packages/native-modules"
  ],
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "build": "npm run mobile:build && npm run web:build",
    "test": "npm run test:mobile && npm run test:web",
    "test:mobile": "npm run test --workspace=apps/mobile",
    "test:web": "npm --prefix apps/web run test --if-present",
    "lint": "npm run lint:mobile && npm run lint:web",
    "lint:mobile": "npm run lint --workspace=apps/mobile",
    "lint:web": "npm --prefix apps/web run lint",
    "typecheck": "npm run typecheck:mobile && npm run typecheck:web",
    "typecheck:mobile": "npm run typecheck --workspace=apps/mobile",
    "typecheck:web": "npm --prefix apps/web run typecheck",
    "format": "npm run format --workspaces --if-present",
    "mobile": "npm -w apps/mobile run dev",
    "web": "npm --prefix apps/web run dev",
    "desktop:dev": "npm --prefix apps/desktop run dev",
    "desktop:build:win": "npm --prefix apps/desktop run build:win",
    "desktop:build:mac": "npm --prefix apps/desktop run build:mac",
    "desktop:build:tauri": "npm --prefix apps/desktop run build",
    "desktop:build:electron:win": "npm --prefix apps/mobile run desktop:build:win",
    "desktop:build:electron:mac": "npm --prefix apps/mobile run desktop:build:mac",
    "mobile:build": "npm -w apps/mobile run build",
    "web:build": "npm --prefix apps/web run build",
    "clean": "npm run clean --workspaces && rm -rf node_modules .turbo",
    "clean:web": "npm --prefix apps/web run clean --if-present",
    "ai:audit": "node scripts/ai-repo-audit.mjs",
    "ai:audit:json": "node scripts/ai-repo-audit.mjs --json",
    "ai:audit:strict": "node scripts/ai-repo-audit.mjs --strict",
    "ai:evidence": "node scripts/ai-evidence-snapshot.mjs",
    "ai:evidence:json": "node scripts/ai-evidence-snapshot.mjs --json",
    "ai:evidence:compare": "node scripts/ai-evidence-compare.mjs --base-ref HEAD^1 --head-ref HEAD",
    "ai:evidence:compare:strict": "node scripts/ai-evidence-compare.mjs --base-ref HEAD^1 --head-ref HEAD --strict",
    "ai:change:manifest": "node scripts/ai-change-manifest.mjs",
    "ai:api:audit": "node scripts/ai-api-contract-audit.mjs",
    "ai:api:audit:strict": "node scripts/ai-api-contract-audit.mjs --strict"
  },
  "devDependencies": {
    "@radix-ui/react-checkbox": "^1.3.11",
    "@tanstack/react-table": "^8.21.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.7",
    "@supabase/supabase-js": "^2.109.0",
    "zustand": "^5.0.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.45.0",
    "recharts": "^3.10.1",
    "tailwind-merge": "^3.7.0"
  }
}
